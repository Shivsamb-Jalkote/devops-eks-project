{{- include "hello-eks.labels" . | nindent 4 }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-eks.fullname" . }}
  labels:
    app: {{ include "hello-eks.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "hello-eks.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "hello-eks.name" . }}
    spec:
      containers:
        - name: {{ include "hello-eks.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.targetPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
